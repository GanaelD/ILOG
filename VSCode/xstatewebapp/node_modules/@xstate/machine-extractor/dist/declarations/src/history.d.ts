import { types as t } from "@babel/core";
interface HistoryNode {
    node: t.Node;
    value: "shallow" | "deep" | boolean;
}
export declare const History: import("./types").AnyParser<HistoryNode>;
export {};
